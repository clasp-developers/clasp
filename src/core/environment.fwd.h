#ifndef _core_environment_fwd_H
#define _core_environment_fwd_H
namespace core
{
    FORWARD(Environment);
    FORWARD(LexicalEnvironment);
    FORWARD(SymbolMacroEnvironment);
    FORWARD(ValueEnvironment);
    FORWARD(BlockEnvironment);
    FORWARD(FunctionValueEnvironment);
    FORWARD(TagbodyEnvironment);
    FORWARD(GlueEnvironment);
}
#endif
