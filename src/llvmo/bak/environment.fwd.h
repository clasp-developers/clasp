#ifndef llvmo_environment_fwd_H
#define llvmo_environment_fwd_H
namespace llvmo
{
    FORWARD(Environment);
    FORWARD(SymbolMacroEnvironment);
    FORWARD(ValueEnvironment);
    FORWARD(BlockEnvironment);
    FORWARD(FunctionEnvironment);
    FORWARD(TagbodyEnvironment);
}
#endif
