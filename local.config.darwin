
export PREFIX = $(HOME)/local/clasp
export TARGET-OS = darwin
export PJOBS = 4
export LINK = static
export EXTERNALS = $(HOME)/local/externals-clasp

#
# Use the builtin clang that came with Xcode6.0.1 because
# the OS X header files don't appear to work with the TOT clang
#
export CLASP_CLANG_PATH = /usr/bin/clang   
